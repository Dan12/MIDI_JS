$(document).ready(function(){
   // create new editor and append it to the body element
   var editor = MIDI_Editor.init("body");
});

// TODO: add global color palette
// TODO: remove white from icons and add it back with fillOval